{
  "name": "lambda-notes",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "description": "Lambda, a self-hosted block-based notes, to-do, and script library.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "engines": {
    "node": ">=22.13"
  }
}
