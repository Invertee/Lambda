# Lambda Apps

Home Assistant app repository for [Lambda](lambda/README.md), a private library for notes and reusable script snippets.

## Installation

1. Open **Settings → Apps → App store** in Home Assistant.
2. Open the repository manager and add `https://github.com/Invertee/Lambda`.
3. Find **Lambda** in the app store and install it.
